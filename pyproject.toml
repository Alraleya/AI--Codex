[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-manga-workbench"
version = "0.1.0"
description = "Local-first deterministic workbench for AI manga episode production"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["backend*"]

